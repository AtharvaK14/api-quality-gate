[pytest]
addopts = -v --alluredir=allure-results -n auto
testpaths = tests
markers =
    smoke: fast subset for CI gate - runs on every push
    regression: full suite - runs nightly
    performance: SLA assertions - enforces per-endpoint response time budgets
